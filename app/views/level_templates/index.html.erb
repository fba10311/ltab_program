<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All levels
    </h1>

    <a href="/levels/new" class="btn btn-block btn-outline-secondary">
      Add a new level
    </a>
  </div>
</div>

<!-- Ransack Code start -->
<div class="row">
  <div class="col-md-12 mb-3">
    <%= render partial: "level_templates/levels_search_form" %>
  </div>
</div>
<!-- Ransack Code end -->

<!-- Location Map Code start -->

<!-- Location Map Code end -->

<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          Stage
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @levels.each do |level| %>
      <tr>
        <!-- Display level.id start -->
        <td>
          <%= level.id %>
        </td>
        <!-- Display level.id end -->

        <!-- Display level.stage start -->
        <td>
          <%= level.stage %>
        </td>
        <!-- Display level.stage end -->

        <!-- Display level.created_at start -->
        <td>
          <%= time_ago_in_words(level.created_at) %> ago
        </td>
        <!-- Display level.created_at end -->

        <!-- Display level.updated_at start -->
        <td>
          <%= time_ago_in_words(level.updated_at) %> ago
        </td>
        <!-- Display level.updated_at end -->

        <td>
          <a href="/levels/<%= level.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>

    <div class="row mb-3">
      <div class="col-md-12">
        <%= paginate @levels, theme: "twitter-bootstrap-4" %>
      </div>
    </div>

  </div>
</div>
